
# Quickstart

This repository is designed to help you repair an entire codebase that was written, modified, or partially generated by AI coding agents.

It is **not** limited to fixing one issue.

The recommended workflow is to audit the whole codebase first, create a prioritized repair backlog, then fix issues one at a time so changes stay safe, reviewable, and testable.

Want your agent to help use this repo? Paste this into your coding agent: 

```text
Read https://github.com/Emmanuel-Bamidele/Fix-AI-Written-Code, use its prompts to audit this codebase, create a prioritized repair backlog, and do not modify files until I approve the first fix.
```

---

## Why not fix everything in one prompt?

Avoid asking an agent to:

```text
Fix the entire codebase.
```

That usually creates a huge, risky diff that is hard to review and easy to break.

Instead, use this loop:

```text
1. Audit the whole codebase.
2. Create a prioritized repair backlog.
3. Fix the highest-priority issue.
4. Add or update tests.
5. Run relevant checks.
6. Summarize the change.
7. Move to the next issue.
8. Repeat until the codebase meets the definition of done.
```

This repo helps with the full process.

---

## Recommended first run

Start with a full triage.

Paste this into your coding agent:

```text
Read `prompts/fix-codebase_triage-and-inventory.md`.

Inspect this repository.

Do not modify files yet.

Return a prioritized triage report with:
- repository structure
- detected stack and frameworks
- package managers
- build system
- test setup
- CI/CD setup
- database and migration setup
- suspicious generated-code patterns
- broken commands
- security risks
- correctness risks
- test gaps
- dependency risks
- architecture risks
- performance risks
- recommended repair order
```

The agent should inspect the codebase and report findings before changing anything.

---

## Step 1: Create the repair backlog

After the triage report, ask the agent to create a full repair backlog.

Paste:

```text
Read `prompts/fix-codebase_master-prompt.md`.

Using the triage report, create a complete repair backlog for this codebase.

Group findings by severity:
- Critical
- High
- Medium
- Low

For each finding, include:
- finding ID
- category
- severity
- affected files
- evidence
- impact
- proposed fix
- required tests
- recommended order

Do not edit files yet.
```

The goal is to get a clear list of everything that needs to be fixed.

---

## Step 2: Start the repair loop

Once you have a repair backlog, fix issues one at a time.

Paste:

```text
Read `prompts/fix-codebase_remediation-workflow.md`.

Take the highest-priority unresolved finding from the repair backlog.

Fix only that finding.

Before editing:
- restate the finding
- explain the evidence
- identify the expected behavior
- identify the required test or reproduction

Then:
- make the smallest safe fix
- add or update tests
- run relevant checks
- summarize files changed
- update the finding status
- recommend the next finding to fix

Do not refactor unrelated code.
Do not fix multiple unrelated findings in the same patch.
```

This starts the main repair loop.

Repeat this step until the important findings are resolved.

---

## Step 3: Prioritize security and data safety

Critical issues should be fixed first.

Use the security prompt for a focused security review:

```text
Read `prompts/fix-codebase_security.md`.

Perform a security review of this repository.

Do not modify files yet.

Return findings for:
- secrets
- authentication
- authorization
- cross-tenant access
- injection
- XSS
- CSRF
- SSRF
- file uploads
- webhooks
- sensitive logging
- dependency vulnerabilities
- missing rate limits

For each finding, include:
- severity
- affected files
- evidence
- impact
- proposed fix
- required negative tests
```

Then fix security findings one by one using the remediation workflow.

---

## Step 4: Fix correctness bugs

After critical security and data-safety issues, review correctness.

Paste:

```text
Read `prompts/fix-codebase_correctness.md`.

Review the repository for correctness issues.

Do not modify files yet.

Look for:
- runtime crashes
- broken edge cases
- missing null checks
- invalid assumptions
- incorrect async behavior
- missing awaits
- swallowed errors
- wrong state transitions
- weak validation
- timezone issues
- currency or decimal issues

Return prioritized findings with:
- severity
- affected files
- evidence
- impact
- proposed fix
- required tests
```

Then fix correctness findings one by one.

---

## Step 5: Repair tests

After the first major fixes, improve the test suite.

Paste:

```text
Read `prompts/fix-codebase_tests.md`.

Inspect the test suite.

Do not modify files yet.

Find:
- fake tests
- shallow tests
- flaky tests
- skipped tests
- tests with no meaningful assertions
- tests that only assert mocks
- missing regression tests
- missing security tests
- missing integration tests

Return a test repair plan ordered by risk.
```

Then repair tests in focused patches.

---

## Step 6: Clean dependencies

Use this after correctness and security are under control.

Paste:

```text
Read `prompts/fix-codebase_dependencies.md`.

Audit dependencies.

Do not modify files yet.

Find:
- unused dependencies
- duplicate libraries
- vulnerable packages
- unnecessary packages
- dev dependencies used as production dependencies
- production dependencies used only for development
- lockfile problems
- abandoned packages
- supply-chain risks

Return a dependency cleanup plan.
```

Then remove or update dependencies carefully.

---

## Step 7: Repair CI/CD

Use this to prevent regressions.

Paste:

```text
Read `prompts/fix-codebase_ci-cd.md`.

Inspect CI/CD configuration.

Do not modify files yet.

Find missing or broken gates for:
- dependency installation
- formatting
- linting
- type checking
- unit tests
- integration tests
- build
- secret scanning
- dependency auditing
- deployment safety

Return a CI/CD repair plan.
```

Then implement CI/CD fixes in focused patches.

---

## Step 8: Review data and migrations

Use this before deploying database changes.

Paste:

```text
Read `prompts/fix-codebase_data-and-migrations.md`.

Review database schema, migrations, and persistence logic.

Do not modify files yet.

Find:
- unsafe migrations
- destructive schema changes
- missing constraints
- missing indexes
- missing transactions
- unsafe backfills
- bad cascade behavior
- timezone risks
- decimal precision risks
- rollback risks

Return a data-safety repair plan.
```

---

## Step 9: Review architecture

Use this when the codebase has inconsistent patterns or multiple agent-created implementations.

Paste:

```text
Read `prompts/fix-codebase_architecture.md`.

Review the repository for architecture drift.

Do not modify files yet.

Find:
- duplicate implementations
- multiple auth systems
- multiple API clients
- multiple config loaders
- multiple validation approaches
- circular dependencies
- business logic in the wrong layer
- dead abstractions
- inconsistent naming
- unclear module boundaries

Return an incremental architecture repair plan.
```

Architecture fixes should usually happen after urgent security and correctness issues.

---

## Step 10: Review performance

Use this after correctness and safety are stable.

Paste:

```text
Read `prompts/fix-codebase_performance.md`.

Review the repository for performance problems.

Do not modify files yet.

Find:
- N+1 queries
- unbounded database queries
- missing pagination
- missing indexes
- unbounded parallelism
- blocking work in request paths
- slow background jobs
- repeated expensive computation
- frontend re-render loops
- large bundles
- unoptimized assets

Return a performance repair plan with evidence where possible.
```

---

## Step 11: Add observability

Use this before production deployment.

Paste:

```text
Read `prompts/fix-codebase_observability.md`.

Review the repository for observability and operations readiness.

Do not modify files yet.

Find missing or weak:
- structured logs
- request IDs
- error tracking
- metrics
- health checks
- readiness checks
- audit logs
- background job visibility
- alerts
- runbooks

Return an observability repair plan.
```

---

## Step 12: Final definition of done

When the repair backlog is mostly complete, run the final acceptance check.

Paste:

```text
Read `prompts/fix-codebase_definition-of-done.md`.

Evaluate whether this repository now meets the definition of done.

Do not modify files.

Return a final hardening report with:
- completed fixes
- remaining risks
- failed or missing checks
- security status
- correctness status
- test status
- CI/CD status
- data and migration status
- observability status
- recommended future work
- confidence level
```

---

## Full-codebase repair loop

Use this repeated loop until the codebase is stable:

```text
Read `prompts/fix-codebase_remediation-workflow.md`.

Take the next highest-priority unresolved finding from the repair backlog.

Fix only that finding.

Add or update tests.

Run relevant checks.

Update the finding status.

Recommend the next finding to fix.
```

Repeat until:

```text
- all Critical findings are fixed
- all High findings are fixed
- Medium findings are fixed or accepted with explanation
- Low findings are fixed or moved to future cleanup
- tests pass
- build passes
- CI passes
- security risks are addressed
- the definition of done is satisfied
```

---

## Recommended order

For most projects, use this order:

```text
1. Triage and inventory
2. Master repair backlog
3. Security review
4. Critical security fixes
5. Data-safety fixes
6. Correctness fixes
7. Test repair
8. Dependency cleanup
9. CI/CD repair
10. API contract review
11. Backend or frontend focused cleanup
12. Architecture cleanup
13. Performance review
14. Observability review
15. Definition of done
```

---

## Severity guide

Use this severity model:

| Severity | Meaning |
|---|---|
| Critical | Secret exposure, auth bypass, data loss, remote code execution, destructive migration, production outage risk |
| High | Broken authorization, incorrect business logic, runtime crashes, major build or test failure |
| Medium | Performance bottleneck, architecture drift, flaky tests, weak error handling |
| Low | Formatting, naming, minor cleanup, documentation gaps |

Fix in this order:

```text
Critical -> High -> Medium -> Low
```

---

## Good agent prompts

Use specific prompts like:

```text
Inspect first. Do not edit files.
```

```text
Create a repair backlog. Do not edit files yet.
```

```text
Fix only the highest-priority unresolved finding.
```

```text
Add a regression test.
```

```text
Run relevant checks and report failures honestly.
```

```text
Do not refactor unrelated code.
```

---

## Bad agent prompts

Avoid vague prompts like:

```text
Fix the whole app.
```

```text
Make this production ready.
```

```text
Clean up everything.
```

```text
Refactor the codebase.
```

```text
Make all tests pass however you can.
```

These prompts are too broad and can produce unsafe changes.

---

## Important note

This repository is for full-codebase repair.

The reason it recommends fixing one issue at a time is safety.

The process is:

```text
Audit everything.
Plan everything.
Fix one thing.
Verify it.
Repeat.
```
